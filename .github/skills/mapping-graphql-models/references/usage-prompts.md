# Usage Prompts

Use these prompts to trigger this skill intentionally.

1. "I need to add a new media detail field from AniList. Map the change from schema to fragment, model variant, remote source, and source implementation."
2. "Refactor this query to use proper fragments and ensure the Kotlin Serializable model hierarchy still matches Core versus Extended responsibilities."
3. "I am adding an edge series field. Show me where it belongs in edge schema mapping versus AniList mapping and how to keep source boundaries clean."
4. "Review this GraphQL and model change for drift: operation names, @GraphQuery mapping, container type alignment, and variant ownership."
