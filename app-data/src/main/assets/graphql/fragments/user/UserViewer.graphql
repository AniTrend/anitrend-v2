fragment UserViewer on User {
    ... UserExtended
    options {
        airingNotifications
        displayAdultContent
        notificationOptions {
            enabled
            type
        }
        timezone
    }
    isBlocked
    unreadNotificationCount
}