fragment UserViewer on User {
    ... UserExtended
    options {
        airingNotifications
        displayAdultContent
        restrictMessagesToFollowing
        notificationOptions {
            enabled
            type
        }
        disabledListActivity {
            type
            disabled
        }
        timezone
    }
    isBlocked
    unreadNotificationCount
}