query GetMediaListEntry(
    $scoreFormat: ScoreFormat!,
    $mediaId: Int!,
    $userId: Int!
) {
    MediaList(
        mediaId: $mediaId,
        userId: $userId
    ) {
        ... MediaListExtended
    }
}