fragment UserProfileReviewPreview on Review {
    id
    summary
    score
    rating
    ratingAmount
    siteUrl
    createdAt
    updatedAt
    mediaId
    mediaType
    media {
        ... UserProfileMediaPreview
    }
}
