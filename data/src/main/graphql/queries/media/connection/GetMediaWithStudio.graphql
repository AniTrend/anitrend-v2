query GetMediaWithStudio(
    $id: Int!,
    $isMain: Boolean,
    $sort: [StudioSort]
) {
    Media(id: $id) {
        id
        studios(sort: $sort, isMain: $isMain) {
            ... MediaStudioConnection
        }
    }
}
