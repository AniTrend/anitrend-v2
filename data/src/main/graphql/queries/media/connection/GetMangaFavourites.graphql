query GetMangaFavourites($id: Int!, $scoreFormat: ScoreFormat, $page: Int!, $perPage: Int!) {
    User(id: $id) {
        favourites {
            manga(page: $page, perPage: $perPage) {
                ... MediaFavouriteConnection
            }
        }
    }
}

