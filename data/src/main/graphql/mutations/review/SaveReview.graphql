mutation SaveReview(
    $id: Int,
    $mediaId: Int,
    $body: String,
    $summary: String,
    $score: Int,
    $isPrivate: Boolean
) {
    SaveReview(
        id: $id,
        mediaId: $mediaId,
        body: $body,
        summary: $summary,
        score: $score,
        private: $isPrivate
    ) {
        ... ReviewCore
    }
}
