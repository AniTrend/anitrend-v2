query NewsConnection($after: String, $before: String, $first: PositiveFloat) {
  newsConnection: news(after: $after, before: $before, limit: $first) {
    count
    first
    last
    data {
      id
      title
      link
      image
      description
      content
      publishedOn
    }
  }
}
