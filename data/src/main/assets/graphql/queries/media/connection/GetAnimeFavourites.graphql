query GetAnimeFavourites($id: Int!, $scoreFormat: ScoreFormat) {
    User(id: $id) {
        favourites {
            anime {
                ... MediaFavouriteConnection
            }
        }
    }
}
