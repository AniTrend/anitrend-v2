query GetMangaFavourites($id: Int!, $scoreFormat: ScoreFormat) {
    User(id: $id) {
        favourites {
            manga {
                ... MediaFavouriteConnection
            }
        }
    }
}
